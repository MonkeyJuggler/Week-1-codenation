const word = "strings"

const index = (word.charAt(0));
    console.log(word.charAt(0));

const last = (word.charAt(6));
    console.log(last);

if (index == last) {
    console.log("True");
}
    else {
    console.log("False");
}
